# Copy to .env or export directly. Never commit a real token.
# Needs "repo" scope (classic) or Contents+Pull requests+Metadata read/write (fine-grained).
GITHUB_TOKEN=ghp_your_personal_access_token_here
