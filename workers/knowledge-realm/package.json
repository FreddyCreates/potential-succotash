{
  "name": "knowledge-realm",
  "version": "1.0.0",
  "description": "KNOWLEDGE-REALM — Knowledge Repository (SAGE) - The memory and learning center of the organism",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "build": "wrangler deploy --dry-run",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare-workers",
    "knowledge-base",
    "rag",
    "memory",
    "sage"
  ],
  "author": "Enterprise-OS",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
