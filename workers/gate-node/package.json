{
  "name": "gate-node",
  "version": "1.0.0",
  "description": "GATE-NODE — Protocol Entry Point (SENTINEL) - The gateway that protects and routes all traffic",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "build": "wrangler deploy --dry-run",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare-workers",
    "gateway",
    "authentication",
    "security",
    "sentinel"
  ],
  "author": "Enterprise-OS",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
