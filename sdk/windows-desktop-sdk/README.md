# @medina/windows-desktop-sdk

> Windows Desktop integration SDK — sovereign organism intelligence for Win32, WinUI, system tray, and native notifications.

## Overview

Provides Windows-native primitives for wiring sovereign intelligence into desktop applications. Every module maps to frontier models, enforces architectural laws, and pulses on the 873ms organism heartbeat.

## Modules

| Module | Primitive Function |
|---|---|
| `SystemTrayAgent` | Manage intelligent system-tray icons with context menus wired to AI engines |
| `NativeNotificationBridge` | Route organism intelligence events to Windows Toast/Action Center notifications |
| `WinUIRenderer` | Render sovereign UI components via WinUI 3 / WebView2 bridge |
| `DesktopShortcutManager` | Create, manage, and AI-enhance desktop shortcuts and jump lists |
| `WindowStateOrchestrator` | Orchestrate window positioning, snapping, and multi-monitor layouts via AI |

## Ring Affinity

**Interface Ring** — organism placement: Frontend organism / Windows desktop layer

## Laws Enforced

AL-028, AL-029, AL-021, AL-004, AL-018
