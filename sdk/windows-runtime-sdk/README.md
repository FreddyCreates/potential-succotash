# @medina/windows-runtime-sdk

> Windows Runtime SDK — sovereign organism intelligence for WinRT, background tasks, App Services, and Windows APIs.

## Overview

Provides Windows Runtime primitives for wiring sovereign intelligence into UWP and WinRT applications. Every module maps to frontier models, enforces architectural laws, and pulses on the 873ms organism heartbeat.

## Modules

| Module | Primitive Function |
|---|---|
| `BackgroundTaskScheduler` | Schedule and manage AI-powered Windows background tasks with adaptive timing |
| `AppServiceBridge` | Create bidirectional App Service connections between AI extensions and host apps |
| `WinRTSensorHub` | Aggregate Windows sensor data (location, light, accelerometer) for organism edge sensing |
| `FileActivationHandler` | Handle file-type activations with AI-powered content routing and processing |
| `ProtocolLaunchRouter` | Route protocol-based launches (URI schemes) through multi-model intelligence |

## Ring Affinity

**Sovereign Ring** — organism placement: Organism core / Windows runtime layer

## Laws Enforced

AL-019, AL-020, AL-034, AL-036, AL-018
